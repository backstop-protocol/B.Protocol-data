{
  "name": "arbitrum_vesta_vst",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ arbitrum_vesta_vst",
    "create-local": "graph create --node http://localhost:8020/ arbitrum_vesta_vst",
    "remove-local": "graph remove --node http://localhost:8020/ arbitrum_vesta_vst",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 arbitrum_vesta_vst",
    "deploy-hosted": "graph deploy --product hosted-service shmuel-web/bprotocol-vesta-eth"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.24.1",
    "web3": "^1.7.1"
  }
}
